- Always commit and push changes to BOTH GitHub repositories after completing a task or update to keep both synchronized:
  1. Web/Core repository: `HBTrack_April02` (`git push origin main`)
  2. iOS repository: `Houbara-Tracker-v2.0-iOS` in `/Users/abdelazizchlih/HBTrack_April02/ios` (`git -C ios commit` and `git -C ios push origin main`)
